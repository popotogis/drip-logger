# 開発ロードマップ

## 機能
- [x] **高度なデータモデル (Freak Mode)**

  - **目標**: コーヒーマニア（フリーク）の要求に応えつつ、一般ユーザーには負担をかけない柔軟な構造。
  - **Beanモデル拡張**:
    - [x] `process` (精製: Washed, Natural etc.) -> `String?`
    - [x] `variety` (品種: Geisha, Bourbon etc.) -> `String?`
    - [x] `roastDate` (焙煎日) -> `DateTime?`

  - **Recipeモデル拡張**:
    - [x] 抽出環境の柔軟性向上。
    - [x] `dripper` (ドリッパー: V60, Kalita etc.) -> `String?`
    - [x] `grinder` (グラインダー: Comandante, EK43 etc.) -> `String?`
    - [x] `filter` (フィルター: Abaca, Stainless etc.) -> `String?`

    - 将来的には `Map<String, String>` で無限に拡張可能にする構想も持つが、まずは主要な3つを実装。

- [x] **レシピ共有機能 (Magic Code)**

  - **目標**: サーバーレスで手軽にレシピを共有する (Plan A)。
  - **仕様**:
    - **Export**: レシピを圧縮・コード化(Base64)し、クリップボードにコピー。
    - **Import**: コードを読み込み、レシピとして復元。
    - **Format**: `drip://recipe/<base64_string>` のような形式を想定。



- [x] **データモデルのリファクタリング (TDD)**
  - **目標**: `Recipe.note` を `String` から `String?` (null許容) に変更する。

  - **アプローチ (テスト駆動)**:
    1. 「`note` が `null` の場合のJSON変換」テストケースを追加し、失敗させる（Red）。
    2. モデルを修正してテストをパスさせる（Green）。
    3. UI側の表示ロジック（`isEmpty` チェックなど）を修正する（Refactor）。


## 技術的改善 (Dart習得への道)
1. [x] **Riverpodの導入**
   - 状態管理を `setState` から `Riverpod` へ移行する。
   - 目的: グローバルな状態共有と、ロジックの分離を学ぶ。

2. [x] **Lintの厳格化**
   - `flutter_lints` の設定を強化し、推奨される記述レイアウトを体に叩き込む。

3. [x] **テストの導入**
   - ユニットテスト (`flutter_test`) を書いてみる。


4. [x] **リポジトリのテスト (Mockの活用)**
   - **目標**: `RecipeRepository` の保存・読み込みロジックをテストする。

   - **手法**:
     - `shared_preferences` のモック機能 (`setMockInitialValues`) を使用。
     - 実機に依存せず、CI/CD環境でも実行可能なテストを作成する。

## 次のフェーズ (Phase 2)
### 機能
- [x] **抽出前レシピ調整 UX**
  - **目標**: 抽出直前に「その日のコンディションや味の調整」に合わせてレシピを動的に調整できるようにする。
  - **仕様**:
    - 抽出開始ボタンの後に、調整画面（豆量・総湯量・ステップごとの湯量・**挽き目・温度など全項目**の変更）を挿入。
    - ステップごとの湯量を自由に変更し、その結果として総湯量が連動する、あるいはその逆の調整を可能にする。
    - 調整した数値でそのまま抽出（Brewing）へ移行する。
    - **抽出完了後、調整されていた場合は「新しいレシピとして保存」する選択肢を提供。**

---

## 🚀 Webアプリ移行 (Next.js Transition)
**目標**: パフォーマンスと安定性向上のため、FlutterからNext.js (Web) へ移行する。
**戦略**: 一括移行ではなく、「Side-by-Side (並行稼働)」で段階的に進める。

### Phase 0: データのクラウド化 (Flutter)
- [x] **Firestore導入**: ローカルDB (Sembast) から Firestore へデータを移行。
- [x] **匿名認証**: ログイン不要で各端末のデータをクラウドに保存可能にする。
- **成果**: Flutterアプリを使いつつ、データがクラウドにある状態を作る。

### Phase 1: 閲覧用Webアプリ (Next.js)
- [x] **Next.jsプロジェクト立ち上げ**: TypeScript, Tailwind CSS, Shadcn/UI。
- [x] **データ閲覧**: Firestoreからレシピ一覧を表示する機能のみ実装。
- **成果**: 「PCの大画面でレシピを見る」といった使い分けが可能になる。

### Phase 2: 機能移植 (Core Features)
- [x] **レシピ作成・編集**: Web上でレシピを作れるようにする。
- [x] **ドリップタイマー**: Web Workers等を用いて、ブラウザでも正確なタイマーを実装する。
- [ ] **ハンズフリー操作 (Future)**: ワイヤレスイヤホンの再生/停止コマンドをフックし、スマホに触れずにステップ進行を行えるようにする。
- **成果**: 利用頻度の高い機能をWebに移し、実用レベルに引き上げる。

### Phase 3: 完全移行 (Shutdown Flutter)
- [x] **全機能のWeb化**: 履歴、豆管理、分析など残り機能を移植。
- [ ] **Flutterアプリ終了**: Webアプリ一本に統合。
